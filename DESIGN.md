# 设计与数据流

No Taste Today 是一个单人听歌记录、多读者浏览的个人音乐刊物。首版不追求全天完整采集，不提供正在播放功能。

## 架构

```text
Spotify 最近播放 → Worker → D1 播放记录
                            ↓
                   程序统计 + 增量音乐分类
                            ↓
                  Kimi 结构化乐评与推荐候选
                            ↓
               Spotify 验证推荐 + 资料引用校验
                            ↓
                     D1 报告 → React 页面
```

手动生成：页面 → D1 任务占用 → Queues → Worker 生成 → D1 报告。提交接口立即返回 202，生成不依赖浏览器连接。队列主动投递，没有周期性查询任务的 Cron。

前端由 Workers Static Assets 托管，与 API 同域。Cron 每天触发，周一、月初额外生成已结束周期的报告。无需单独的 Pages 项目。首版不启用报告 Cache API，避免私人报告和管理状态被错误缓存；历史记录、分类和资料直接持久化到 D1。

## 报告结构

实际类型和模型约束见 `shared/schema.ts`。

| 字段                  | 用途                                 | 来源                               |
| --------------------- | ------------------------------------ | ---------------------------------- |
| `metrics`、`previous` | 本期／上期统计、曲风占比、时段、年代 | 程序计算；曲风分类由模型提供       |
| `taste_comment`       | 面向旁观读者的审美短评               | Kimi                               |
| `taste_profile`       | 风格概括与标签                       | Kimi                               |
| `discoveries`         | 歌曲之间的联系、反差和有依据的发现   | Kimi，引用输入曲目                 |
| `fun_facts`           | 歌曲背景故事与来源                   | 已抓取资料 + Kimi，校验引用片段    |
| `recommendations`     | 顺着口味、跨一步、意外之选           | Kimi 候选 + Spotify 歌名／歌手匹配 |
| `taste_evolution`     | 与上期相比的变化                     | 两期有有效样本时才生成             |

播放事件按 `track_id + played_at` 去重。重听比例是本期已收集事件中的重复比例，不是用户全部历史的熟悉度。合作歌手均分一条播放；发行日期表示当前版本日期。记录不提供真实听歌分钟数、跳过率或完播率。

音乐标签和乐评属于模型推测，不是音频分析。Fun Fact 只能引用输入资料；片段验证不保证模型对资料的解释绝对正确。资料获取失败时省略故事。

## 权限与成本

- 公开站点的普通访问者只能手动生成当地当天日报，所有人共享三小时冷却。
- 主人通过 Access JWT 验证和邮箱匹配后获得管理会话，可重新生成历史日／周／月报告。
- 本地读取权限不等于主人验证；Spotify 连接和免冷却操作仍要求登录。
- 任务占用与冷却在同一条数据库条件写入中完成，避免并发重复调用模型。失败也计入冷却。
- 队列每批一条、并发一；每次任务带独立 `run_id`，数据库原子领取防止重复投递再次调用模型。失败不自动重试，排队和运行状态超过 20 分钟允许回收。
- 自动任务不受手动冷却限制，已完成的周期保持幂等；进行中的周期保存为可更新预览。
- Spotify 刷新令牌加密保存，失败时可触发 Telegram 去重提醒。

## 当前边界

Spotify 返回的最近记录可能不完整；历史报告只能分析已保存的数据。来源抓取依赖 Wikipedia 可用性。没有通用失败通知，只有 Spotify 授权相关提醒。Free Worker 的 CPU 和 D1 限额需要上线后观察。评论、免登录评分与多账户托管未实现。
